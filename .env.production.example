# ===================================================================
# Variables de entorno NECESARIAS para PRODUCCIÓN
# ===================================================================
# Copia este archivo y ajusta los valores según tu servidor

# --- SMTP para formulario de contacto ---
GMAIL_USER=devweb.venta@gmail.com
GMAIL_APP_PASSWORD=tu_password_app_aqui
CONTACT_TO=devweb.venta@gmail.com

# --- WordPress API ---
WP_API_BASE=https://api.artenlaclase.cl/wp-json/wp/v2
PUBLIC_WP_MEDIA_ROOT=https://api.artenlaclase.cl

# --- Proxy de imágenes (ACTIVAR si hay problemas de hotlinking) ---
# Si las imágenes del blog no cargan, activa el proxy:
PUBLIC_IMAGE_PROXY=on

# Seguridad del proxy (opcional pero recomendado):
# Genera un secreto aleatorio con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
IMAGE_PROXY_SECRET=genera_un_secreto_aleatorio_de_64_caracteres_aqui

# Rate limiting del proxy (valores por defecto):
IMAGE_PROXY_RATE=120
IMAGE_PROXY_WINDOW_MS=300000

# Hosts permitidos para el proxy:
PUBLIC_IMAGE_PROXY_ALLOW=api.artenlaclase.cl|artenlaclase.cl

# --- Analytics ---
PUBLIC_UMAMI_WEBSITE_ID=d42696c6-7d19-4a9f-9cde-29540ce5e907
PUBLIC_UMAMI_SCRIPT_URL=https://cloud.umami.is/script.js

# --- URL del sitio ---
SITE_URL=https://artenlaclase.cl
