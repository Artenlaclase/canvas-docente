---
import BaseLayout from "../layouts/BaseLayout.astro";
import Header from "../components/Header.astro";
import { Image } from "astro:assets";
import pintura from "../assets/images/pintura.jpg";
import collage from "../assets/images/collage.jpg";
import audiovisual from "../assets/images/audiovisual.jpg";
import mosaico from "../assets/images/mosaico.jpg";
import volumen from "../assets/images/volumen.jpg";
import mascara from "../assets/images/mascara.jpg";
import ContactForm from "../components/ContactForm.astro";
import BlogCard from "../components/BlogCard.astro";
import { getCollection } from 'astro:content';
import { getWpBase, safeListWpPosts } from "../utils/wp";

// Reunir un pool de posts
let pool: any[] = [];
const wpBase = getWpBase();
if (wpBase) {
  pool = await safeListWpPosts(40);
}
if (!pool.length) {
  const locals = await getCollection('blog');
  pool = locals
    .sort((a,b) => +new Date(b.data.date) - +new Date(a.data.date))
    .slice(0, 40);
}

function pickRandom<T>(arr: T[], n: number): T[] {
  const a = [...arr];
  for (let i = a.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [a[i], a[j]] = [a[j], a[i]];
  }
  return a.slice(0, n);
}

// Primer bloque (hasta 3)
let columns: any[] = [];
if (pool.length && typeof pool[0]?.id === 'number') {
  const catMap = new Map<string, any[]>();
  for (const p of pool) {
    const cats = p.data?.categories || [];
    for (const c of cats) {
      const key = c?.name || c?.slug || String(c?.id || '');
      if (!key) continue;
      if (!catMap.has(key)) catMap.set(key, []);
      catMap.get(key)!.push(p);
    }
  }
  const allCats = Array.from(catMap.keys());
  const chosenCats = pickRandom(allCats, Math.min(3, allCats.length));
  columns = chosenCats.map((key) => {
    const arr = catMap.get(key) || [];
    const [post] = pickRandom(arr, 1);
    return { title: key, post };
  }).filter(x => x && x.post);
}
if (columns.length < 3) {
  const rest = pickRandom(pool, 3 - columns.length);
  columns = columns.concat(rest.map((p) => ({ title: 'Notas', post: p })));
}

// Segundo bloque (otros 3 distintos del primero si hay)
let extraColumns: any[] = [];
if (pool.length) {
  const used = new Set(columns.map(c => c.post?.slug));
  const remaining = pool.filter(p => !used.has(p.slug));
  const pick = remaining.slice(0, 3); // orden natural (ya ordenado por fecha)
  extraColumns = pick.map(p => ({ title: (p.data?.categories?.[0]?.name) || 'Notas', post: p }));
}
---

<BaseLayout title="Canvas Docente" description="20 años de artes visuales en aula">
	<Fragment slot="header">
		<Header />
	</Fragment>
	<section class="py-8 sm:py-14">
		<div class="grid gap-8 sm:grid-cols-2 items-center">
			<div>
				<h1 class="text-3xl sm:text-4xl font-bold tracking-tight">20 años de proyectos y técnicas artísticas</h1>
				<p class="mt-3 text-slate-600">Pintura, collage, audiovisual, mosaico, volumen, máscaras, lámparas, diseño y dibujo. Un archivo vivo del trabajo en clases de artes visuales.</p>
				<div class="mt-6 flex gap-3">
					<a href="/galeria" class="inline-flex items-center justify-center rounded-md bg-sky-600 px-5 py-2.5 text-white hover:bg-sky-700">Ver galería</a>
					<a href="/notas" class="inline-flex items-center justify-center rounded-md border border-slate-300 px-5 py-2.5 text-slate-800 hover:bg-slate-50">Leer notas</a>
				</div>
			</div>
							<div class="grid grid-cols-3 gap-3">
								<Image src={pintura} alt="Pintura" class="rounded-lg object-cover h-28 sm:h-40 w-full" />
								<Image src={collage} alt="Collage" class="rounded-lg object-cover h-28 sm:h-40 w-full" />
								<Image src={audiovisual} alt="Audiovisual" class="rounded-lg object-cover h-28 sm:h-40 w-full" />
								<Image src={mosaico} alt="Mosaico" class="rounded-lg object-cover h-28 sm:h-40 w-full" />
								<Image src={volumen} alt="Volumen" class="rounded-lg object-cover h-28 sm:h-40 w-full" />
								<Image src={mascara} alt="Máscaras" class="rounded-lg object-cover h-28 sm:h-40 w-full" />
							</div>
		</div>
	</section>

	<section class="pt-2 -mt-2">
		<h2 class="text-xl font-semibold mb-3 text-center">Explora por técnica</h2>
		<div class="max-w-4xl mx-auto rounded-xl border border-slate-200 p-3 sm:p-4">
			<div class="flex flex-wrap justify-center gap-2">
					{['Pintura','Collage','Audiovisual','Mosaico','Volumen','Máscaras','Lámparas','Diseño','Dibujo'].map((t) => (
						<a href={`/galeria/tecnica/${encodeURIComponent(t)}`} class="rounded-full border border-slate-300 px-4 py-1.5 text-sm hover:bg-slate-50">{t}</a>
				))}
			</div>
		</div>
	</section>

  {columns.length ? (
    <section class="pt-6 mt-24 sm:mt-32 lg:mt-40">
      <h2 class="text-xl font-semibold mb-4">Lecturas</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        {columns.slice(0,3).map(({ title, post }) => (
          <div class="space-y-2">
            <h3 class="text-sm uppercase tracking-wide text-slate-500">{title}</h3>
            <BlogCard post={post} />
          </div>
        ))}
      </div>
    </section>
  ) : null}

  {extraColumns.length ? (
    <section class="pt-6 mt-16 sm:mt-24 lg:mt-28">
      <h2 class="text-xl font-semibold mb-4">Más lecturas</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        {extraColumns.slice(0,3).map(({ title, post }) => (
          <div class="space-y-2">
            <h3 class="text-sm uppercase tracking-wide text-slate-500">{title}</h3>
            <BlogCard post={post} />
          </div>
        ))}
      </div>
    </section>
  ) : null}

	<!-- Contact form with social icons at side -->
  <!-- Separador visual antes del formulario de contacto -->
  <section id="contacto" class="mt-24 sm:mt-32 lg:mt-36">
    <div class="h-px w-full bg-gradient-to-r from-transparent via-slate-300 to-transparent mb-12" aria-hidden="true"></div>
    <div class="max-w-5xl mx-auto rounded-xl border border-slate-200/80 bg-slate-50/60 backdrop-blur-sm p-6 sm:p-8 shadow-sm">
      <div class="grid gap-8 md:grid-cols-12 items-start">
        <!-- Aside redes sociales -->
        <aside class="md:col-span-2 flex md:flex-col justify-center md:justify-start items-center gap-6 md:pt-2 md:sticky md:top-28" aria-label="Redes sociales">
          <a href="https://www.instagram.com/artenlaclase/" target="_blank" rel="noopener noreferrer" class="group text-slate-600 dark:text-slate-600 hover:text-pink-500 dark:hover:text-pink-500 transition-colors" aria-label="Instagram">
            <svg class="h-10 w-10" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path fill-rule="evenodd" d="M12.315 2c2.43 0 2.784.013 3.808.06 1.064.049 1.791.218 2.427.465a4.902 4.902 0 011.772 1.153 4.902 4.902 0 011.153 1.772c.247.636.416 1.363.465 2.427.048 1.067.06 1.407.06 4.123v.08c0 2.643-.012 2.987-.06 4.043-.049 1.064-.218 1.791-.465 2.427a4.902 4.902 0 01-1.153 1.772 4.902 4.902 0 01-1.772 1.153c-.636.247-1.363.416-2.427.465-1.067.048-1.407.06-4.123.06h-.08c-2.643 0-2.987-.012-4.043-.06-1.064-.049-1.791-.218-2.427-.465a4.902 4.902 0 01-1.772-1.153 4.902 4.902 0 01-1.153-1.772c-.247-.636-.416-1.363-.465-2.427-.047-1.024-.06-1.379-.06-3.808v-.63c0-2.43.013-2.784.06-3.808.049-1.064.218-1.791.465-2.427a4.902 4.902 0 011.153-1.772A4.902 4.902 0 015.45 2.525c.636-.247 1.363-.416 2.427-.465C8.901 2.013 9.256 2 11.685 2h.63zm-.081 1.802h-.468c-2.456 0-2.784.011-3.807.058-.975.045-1.504.207-1.857.344-.467.182-.8.398-1.15.748-.35.35-.566.683-.748 1.15-.137.353-.3.882-.344 1.857-.047 1.023-.058 1.351-.058 3.807v.468c0 2.456.011 2.784.058 3.807.045.975.207 1.504.344 1.857.182.466.399.8.748 1.15.35.35.683.566 1.15.748.353.137.882.3 1.857.344 1.054.048 1.37.058 4.041.058h.08c2.597 0 2.917-.01 3.96-.058.976-.045 1.505-.207 1.858-.344.466-.182.8-.398 1.15-.748.35-.35.566-.683.748-1.15.137-.353.3-.882.344-1.857.048-1.055.058-1.37.058-4.041v-.08c0-2.597-.01-2.917-.058-3.96-.045-.976-.207-1.505-.344-1.858a3.097 3.097 0 00-.748-1.15 3.098 3.098 0 00-1.15-.748c-.353-.137-.882-.3-1.857-.344-1.023-.047-1.351-.058-3.807-.058zM12 6.865a5.135 5.135 0 110 10.27 5.135 5.135 0 010-10.27zm0 1.802a3.333 3.333 0 100 6.666 3.333 3.333 0 000-6.666zm5.338-3.205a1.2 1.2 0 110 2.4 1.2 1.2 0 010-2.4z" clip-rule="evenodd"/></svg>
          </a>
          <a href="https://www.youtube.com/artenlaclase" target="_blank" rel="noopener noreferrer" class="group text-slate-600 dark:text-slate-600 hover:text-red-500 dark:hover:text-red-500 transition-colors" aria-label="YouTube">
            <svg class="h-10 w-10" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path fill-rule="evenodd" d="M19.812 5.418c.861.23 1.538.907 1.768 1.768C21.998 8.746 22 12 22 12s0 3.255-.418 4.814a2.504 2.504 0 0 1-1.768 1.768c-1.56.419-7.814.419-7.814.419s-6.255 0-7.814-.419a2.505 2.505 0 0 1-1.768-1.768C2 15.255 2 12 2 12s0-3.255.417-4.814a2.507 2.507 0 0 1 1.768-1.768C5.744 5 11.998 5 11.998 5s6.255 0 7.814.418ZM15.194 12 10 15V9l5.194 3Z" clip-rule="evenodd"/></svg>
          </a>
          <a href="https://www.facebook.com/artenlaclase" target="_blank" rel="noopener noreferrer" class="group text-slate-600 dark:text-slate-600 hover:text-blue-500 dark:hover:text-blue-500 transition-colors" aria-label="Facebook">
            <svg class="h-10 w-10" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path fill-rule="evenodd" d="M22 12c0-5.523-4.477-10-10-10S2 6.477 2 12c0 4.991 3.657 9.128 8.438 9.878v-6.987h-2.54V12h2.54V9.797c0-2.506 1.492-3.89 3.777-3.89 1.094 0 2.238.195 2.238.195v2.46h-1.26c-1.243 0-1.63.771-1.63 1.562V12h2.773l-.443 2.89h-2.33v6.988C18.343 21.128 22 16.991 22 12z" clip-rule="evenodd"/></svg>
          </a>
          <a href="https://www.linkedin.com/in/raulrosalesrebolledo/" target="_blank" rel="noopener noreferrer" class="group text-slate-600 dark:text-slate-600 hover:text-blue-600 dark:hover:text-blue-600 transition-colors" aria-label="LinkedIn">
            <svg class="h-10 w-10" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path fill-rule="evenodd" d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z" clip-rule="evenodd"/></svg>
          </a>
        </aside>
        <!-- Contenedor formulario -->
        <div class="md:col-span-10">
          <h2 class="sr-only">Contacto</h2>
          <ContactForm embedded />
        </div>
      </div>
    </div>
  </section>
  <script>
    // Si venimos de la página de privacidad con intención de volver al formulario
    try {
      if (sessionStorage.getItem('scrollToContact') === '1') {
        sessionStorage.removeItem('scrollToContact');
        const target = document.getElementById('contacto');
        if (target) {
          // pequeño retraso para asegurar layout estable
          setTimeout(() => {
            target.scrollIntoView({ behavior: 'smooth', block: 'start' });
            // Intentar enfocar el primer campo del formulario
            const firstInput = target.querySelector('input, textarea, select, button') as HTMLElement | null;
            if (firstInput) firstInput.focus({ preventScroll: true });
          }, 60);
        }
      }
    } catch {}
  </script>
</BaseLayout>
